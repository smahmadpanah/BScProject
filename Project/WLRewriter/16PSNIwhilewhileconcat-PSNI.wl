program; 
inH h1, h2; 
inL l1, l2; 
 if l2 > 3 then 
l1 = h2 + h1 endif; 
 if ((l2 > 3)) or ((l2 > 3)) then 
outL BOT else 
outL l1 endif ; 
while h1 == 0 do 
l1 = l1 + h1 done ; 
if (h2 < 0) or (l1 >= h2) then 
while h2 > l1 do 
l1 = l1 - h2 done 
endif; 
 if TRUE then 
NOP else 
outL l1 endif 